import "./ItemListConteiner.css"
import{ItemList} from "./ItemList"


export const ItemListConteiner = (props) => {
        <div>
            <h2 className="greeting">{props.saludo}</h2>
        </div>

    

    return(
        <ItemList/>
    )}
